# Copy this file to .env.local and fill in your values

# API Base URL — points to your auth/backend service (via Traefik in Docker, or direct in dev)
NEXT_PUBLIC_API_URL=http://localhost

# WebSocket Gateway URL — points to your websocket-gateway service
NEXT_PUBLIC_SOCKET_URL=http://localhost

# Environment
NODE_ENV=development
